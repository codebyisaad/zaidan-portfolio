version: '3.8'

services:
  portfolio:
    build: .
    image: astro-bento-portfolio
    ports:
      - "4321:4321"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
